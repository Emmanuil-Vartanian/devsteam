// import { useTranslation } from 'react-i18next'

// import { getLangOptions } from 'services/formOptions'
// import { getLanguageSelector } from 'containers/App/store/reducers/selectors'

const useCurrentLang = () => {
  // const { t } = useTranslation()
  // const profileLanguage = useSelector(getLanguageSelector)
  // const langOptions = getLangOptions(t)

  // const currentLang = langOptions?.filter(item => item.value === profileLanguage)[0]

  // return { currentLangCode: currentLang.value.toString().toLowerCase() }
  return {}
}

export default useCurrentLang
